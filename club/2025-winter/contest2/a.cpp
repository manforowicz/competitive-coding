#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n, q; cin >> n >> q;

    // floss like a boss!
    vector<vector<int>> people(n);

    for (vector<int>& person : people) {
        int their_boss; cin >> their_boss;
        person.push_back(their_boss);
    }

    while ()

    for (vector<int>& person : people) {

    }



}
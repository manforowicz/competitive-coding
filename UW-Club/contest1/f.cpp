#include <bits/stdc++.h>
using namespace std;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	freopen("in.txt", "r", stdin);
	
	int n, q; cin >> n >> q;
	
	// MAKE SEGMENT TREE
	
	
	for (int i = 0; i < q; i++) {
		int l, r; cin >> l >> r;
		// TODO
	}
}
